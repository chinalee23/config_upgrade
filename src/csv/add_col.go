package csv

import (
	"common"
	// "env"
	// "fmt"
	// "path/filepath"
)

func add_col(upg *common.STOneUpgrade) {
	// fpath := filepath.Join(env.CurrRegionRoot, "CSV/"+upg.Item+".csv")
	// if !common.IsPathExist(fpath) {
	// 	fmt.Println("add_col", "file not eixst", upg.Item)
	// 	return
	// }

	// patterns := common.ParsePattern(upg.Data)
	// field, ok := patterns["field"]
	// if !ok {
	// 	fmt.Println("add_col, must have pattern [field]")
	// 	return
	// }

	// fmt.Println("add_cok, add", field)
}
